# =============================================================================
# Faux|Dash Environment Configuration
# =============================================================================
#
# Most settings are configured in Admin > Settings after deployment.
# This file contains minimal required settings for Docker deployment.
#
# =============================================================================

# -----------------------------------------------------------------------------
# Optional: Authentication URL
# -----------------------------------------------------------------------------

# Your deployment URL (change if using reverse proxy with different URL)
NEXTAUTH_URL=http://localhost:8080

# -----------------------------------------------------------------------------
# Optional: Authentication Secret
# -----------------------------------------------------------------------------

# If not provided, a secret is auto-generated and stored in /data/.nextauth_secret
# To provide your own: openssl rand -base64 32
# NEXTAUTH_SECRET=your-secret-here

# -----------------------------------------------------------------------------
# Optional: Container Settings
# -----------------------------------------------------------------------------

# User/Group ID for file permissions (run `id` to check yours)
# PUID=1000
# PGID=1000

# -----------------------------------------------------------------------------
# Configured In Admin > Settings
# -----------------------------------------------------------------------------
#
# The following are all configured via the web UI:
#
# - Redis cache server
# - Weather API keys and providers
# - SMTP/Email configuration
# - OIDC/SSO authentication
# - GeoIP analytics
# - Theme and appearance
# - Search engine preferences
#
# =============================================================================
